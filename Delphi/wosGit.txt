GITHUB

## ** Configurar o Git ** ##
git config --global --replace-all credential.helper wincred
git config --global --replace-all credential.useHttpPath true
git config --global --replace-all gui.encoding utf-8

git config --global --replace-all user.name "Willian Santos"
git config --global --replace-all user.email "williansantos__@live.com"

rem git config --global --replace-all core.autocrlf true
git config --global --replace-all core.safecrlf false
git config --global commit.gpgsign false

#GitHub (*sempre usar da Branch "develop")

Clonar Repositório
git clone -b develop https://github.com/wos83/wosGeminiAPI.git

Tarefa: Ajuste do Token

Clonar minha Branch
git clone -b fix/ajuste do token https://github.com/wos83/wosGeminiAPI.git

Criar a Branch apartir da "develop"
fix/ajuste do token

git branch -M fix/ajuste-do-token

git add .
git commit -m "fix/ajuste do token"
git push origin fix/ajuste-do-token

**** TEXTO DO PULL REQUEST ****

### Contexto :memo:

> Descreva um pouco do porquê esse Pull Request foi criado.
#### Finalizei a tarefa.

### O que foi alterado :pencil2:

> Descreva as alterações que foram feitas no código. Nessa sessão podem conter
> imagens e outros materiais sobre as alterações visuais.
#### Ajustei o a implementação do Token.

### Como testar :gear:

> Descreva como o Pull Request pode ser testado e qual o resultado esperado.
#### Abrir o App e atualizar os Tokens.

### Dependências :link:

> Descreva as dependências que o Pull Request precisa ter para funcionar.
#### Não tem nenhuma.

Relacionado à tarefa [Ajuste do Token](https://github.com/dsdanielpark/Gemini-API/blob/main/documents/README_DEV.md#multi-cookie-Gemini).


-----------------------------------------------------------------------------------------------------------------------------------------------------------

Pull Request Criado no Github:
https://github.com/wos83/wosGeminiAPI/pull/6

-----------------------------------------------------------------------------------------------------------------------------------------------------------

**** MERGE DO MEU CODIGO PARA A DEVELOP ****

Selecione a branch "develop"
git branch -M develop

git add .
git commit -m "merge fix/ajuste-do-token into develop"
git push develop fix/ajuste-do-token

-----------------------------------------------------------------------------------------------------------------------------------------------------------